---
name: test
description: Run the project test suite (or a single test) and systematically fix any failures.
argument-hint: "[testName]"
---

Use the `run-and-fix-tests` skill to build, run tests, and fix failures. If a specific test name is provided in the argument ($1), run only that test. Otherwise, run all tests. 
