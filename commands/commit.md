---
name: commit
description: Create git commit with Claude Code attribution cost metrics embedded as git trailers.
---

Use the `write-git-commit` skill to create a git commit with Claude Code attribution and cost metrics embedded in the git trailers. If the argument is "now" ($1), activate fast mode to skip all approval prompts.
